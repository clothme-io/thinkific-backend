import { getRepository } from "typeorm";
import { Tenant } from "../../../entity/Tenant";

export const checkIfEmployeeTenantExist = async (data: any) => {};

export const checkIfEmployeeTenantExist2 = async (employeeDetail: any) => {};
